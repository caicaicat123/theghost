# 更新记录

## 1.5.0

- **项目更名为 The Ghost（幽灵）**：README 标题、游戏内标题、命令提示、AI 人设提示词里所有「回声」都改成「幽灵」
  （此前聊天前缀已经是「幽灵」，但人设还自称回声，它会答错自己的名字）
- **插件 ID 改为 `TheGhost`**：Bukkit 只允许插件名用 `A-Za-z0-9 _.-`，中文名会直接导致插件加载失败，
  所以内部标识用英文。数据目录随之变成 `plugins/TheGhost/`，jar 名变成 `theghost-<版本>.jar`
- **管理命令改为 `/ghost`**，旧的 `/mcbot` 保留为别名；权限节点新增 `theghost.admin` / `theghost.talk`，
  旧的 `mcbot.admin` / `mcbot.talk` 继续被承认（服务器不用改 LuckPerms）
- 升级注意：改了插件名之后 Bukkit 会用新的数据目录，把旧的 `plugins/McBot/`
  （含 `config.yml` 与 `grudge.yml`）复制一份到 `plugins/TheGhost/` 就行，配置和 API Key 不用重填

## 1.4.0

- **它会真的捉弄人了**：新增三个真动作——`LIGHTNING` 在玩家头上劈闪电、
  `CREEPER` 在玩家身后生成锁定他的苦力怕、`JUMPSCARE` 贴脸音效 + 屏幕闪字
- **记仇系统**：按玩家 UUID 记账（`grudge.yml`，重启不丢）。骂它加分、夸它减分、每小时自然淡忘；
  分数分 0–3 级，等级决定它能动用哪档捉弄，等级越高也越容易被它点名
- **AI 参与判定但不额外花钱**：模型在同一次回复里多输出一行 `ANGER: 0-5`，
  表示"这句话有多冒犯"，插件最多采信 2 分，防止模型情绪化乱记账
- **默认强度按用户要求拉满**：闪电真掉血、可致死；苦力怕真爆炸、会炸方块、可炸死人。
  闪电默认不点火（`prank.lightning.set-fire` 可开）
- 三道安全闸：记仇等级不够不执行、闪电/苦力怕 45 秒冷却、`prank.*.enabled` 开关。
  `prank.enabled: false` 一键退回"只动嘴"版本
- 新增管理指令：`/mcbot grudge [玩家] [set <分>|reset]`、`/mcbot prank <玩家> [动作]`
- 插件的苦力怕带 `mcbot_prank` 标记：插件卸载/关服时会清掉它们；
  `break-blocks: false` 时只清爆炸方块列表，不动玩家自己放的苦力怕
- 修复：模型用中文写法 `动作：CREEPER`（全角冒号）时动作解析失败

## 1.3.1

- 修正动作解析：模型有时会漏掉 `SOUND` 关键字，直接写音效名（`ACTION: block.note_block.pling`），
  此前会被判为无效动作而静默失败。现在这种写法会被识别成播放音效
- 人设提示里补上 SOUND 关键字的正确写法示例

## 1.3.0

- 网络加固：连接超时从 10 秒改为 8 秒并**自动重试**（`deepseek.retries`，默认 1 次），
  偶发 `HttpConnectTimeoutException` 不再直接掉进本地台词
- 显式禁用代理（`ProxySelector.of(null)`），避免服务器上其他插件设置过 `https.proxyHost` 导致连接卡死
- 启动时自动做一次 DeepSeek 连通性检查，结果写进控制台
- 新增 `/mcbot test [内容]`：管理员可直接打一次 API，成功与失败都记进日志
- 修正人设：明确告诉模型「@yl / @幽灵 指的就是它自己」，此前它会把 @yl 当成另一个玩家

## 1.2.1

- 修正 `/mcbot status` 里 @ 词重复显示的问题（此前显示成 `@@yl`）

## 1.2.0

- 本地台词从 10 句扩到 110 句，按性格分成五类：阴森、嘴贫、观察玩家、自言自语、偶尔正经
- 台词全部去重，并经 SnakeYAML（服务端同款解析器）校验可正常加载

## 1.1.1

- 被 `@` 或使用 `/mcbot ask` 时**强制调用 API**，不再被 `bot.ai-chance` 概率过滤掉
  （此前这两个场景有 65% 概率只念本地台词）
- `bot.ai-chance` 的作用范围收窄为仅公屏自言自语

## 1.1.0

- 公屏说话人前缀改为「幽灵：」，并支持自定义
- 新增 @ 唤醒机制：只有聊天中出现 `@yl` / `@幽灵` 才会回复，其余聊天完全不检测，避免无意义的 API 调用
- 新增引导接话：公屏说完话后盯住接下来 90 秒内的第一条玩家发言，用固定文案告诉对方怎么找它，然后关闭窗口
- 新增 `mention` 与 `guide` 配置段；`chat-trigger` 配置段被取代
- 修复：硬拦截清单中重复的 `restart` 会让 `Set.of` 在类初始化时抛异常，导致每次说话都报错

## 1.0.0

- 首个版本：定时公屏搞怪、动作白名单（音效 / 标题 / 指令）、DeepSeek 接入、`/mcbot` 指令
